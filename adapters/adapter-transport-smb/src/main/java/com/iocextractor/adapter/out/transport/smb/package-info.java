/**
 * SMB outbound transport adapter backed by SMBJ.
 *
 * <p>The package hides all SMBJ sessions and handles behind the transport-neutral
 * {@code FileTransport} port. Application code must not depend on SMBJ classes.
 */
package com.iocextractor.adapter.out.transport.smb;
