package com.iocextractor.diagnostics;

/**
 * High-level processing area where a diagnostic was produced.
 */
public enum DiagnosticCategory {
    CONFIG,
    SOURCE,
    EXTRACTION,
    CLASSIFY,
    SINK,
    PIPELINE
}
