package com.iocextractor.application.observation;

/** Delivery path that originally registered a canonical precedence rank. */
public enum ObservationOrigin {
    DOCUMENT,
    MANAGED_IMPORT,
    ONESHOT
}
