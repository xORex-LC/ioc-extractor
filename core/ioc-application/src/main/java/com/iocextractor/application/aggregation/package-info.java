/**
 * Storage-neutral artifact row identity, canonical artifact snapshots and
 * run-ledger recovery model. CSV and filesystem details remain behind driven
 * ports.
 */
package com.iocextractor.application.aggregation;
